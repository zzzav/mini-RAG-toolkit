# День 3 — mail_report (парсинг JSON писем и агрегация)

## Что сделали
- Реализовали `src/mail_report.py`:
  - парсинг JSON с письмами
  - фильтр по домену: `endswith("@domain")`
  - флаг `skip-empty-from` для пропуска писем без отправителя
  - группировка по отправителям
  - сортировка по (-count, from)
  - вывод TOP-3 отправителей

## Тесты
- Покрыли логику тестами (валидации, фильтр домена, сортировка, топ).

## Результат
Отработали паттерн: “прочитать данные → отфильтровать → агрегировать → стабильно отсортировать → вывести”.
